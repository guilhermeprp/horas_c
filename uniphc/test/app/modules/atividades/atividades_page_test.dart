import 'package:flutter_test/flutter_test.dart';


main() {
  testWidgets('AtividadesPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AtividadesPage(title: 'Atividades')));
    //  final titleFinder = find.text('Atividades');
    //  expect(titleFinder, findsOneWidget);
  });
}
