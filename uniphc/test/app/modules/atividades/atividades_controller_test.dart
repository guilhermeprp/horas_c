import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:uniphc/app/modules/atividades/atividades_module.dart';

void main() {
  initModule(AtividadesModule());
  // AtividadesController Atividades;
  //
  setUp(() {
    //     Atividades = AtividadesModule.to.get<AtividadesController>();
  });

  group('AtividadesController Test', () {
    //   test("First Test", () {
    //     expect(Atividades, isInstanceOf<AtividadesController>());
    //   });

    //   test("Set Value", () {
    //     expect(Atividades.value, equals(0));
    //     Atividades.increment();
    //     expect(Atividades.value, equals(1));
    //   });
  });
}
