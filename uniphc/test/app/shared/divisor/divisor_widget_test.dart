import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:uniphc/app/shared/divisor/divisor_widget.dart';

main() {
//  testWidgets('DivisorWidget has message', (WidgetTester tester) async {
//    await tester.pumpWidget(buildTestableWidget(DivisorWidget()));
//    final textFinder = find.text('Divisor');
//    expect(textFinder, findsOneWidget);
//  });
}
